<div>
  <div class="flex justify-center p-3 text-blue-950">
  </div>

  <div class="flex row">
    <% @cards.each do |card| %>
      <div class="p-1">
        <%= image_tag card.image_url, alt: card.name, class: "w-[244px] h-[340px] object-cover rounded-xl" %>
      </div>
    <% end %>
  </div>
</div>
