<div class="card">
  <%= link_to image_tag(card.photo_url), card_path(card.id) %>
  <p><strong><%= card.name %></strong>
  <p><%= card.card_type %></p>
  <p>CMC: <%= card.convertedManaCost %></p>
</div>
